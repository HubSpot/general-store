NODE_ENV=development \
browserify \
  --standalone HSStore \
  -t [ reactify --es6 --strip-types ] \
  -t envify \
  src/HSStore.js | uglifyjs --beautify indent-level=2 --comments all -o build/hs-store.js
