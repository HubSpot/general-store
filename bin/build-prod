NODE_ENV=production \
browserify \
  --standalone HSStore \
  -p bundle-collapser/plugin \
  -t [ reactify --es6 --strip-types ] \
  -t envify \
  -t uglifyify \
  src/HSStore.js | uglifyjs -c -o dist/hs-store.min.js

